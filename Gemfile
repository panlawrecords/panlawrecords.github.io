# Gemfile

source "https://rubygems.org"

# Use the GitHub Pages gem (this locks Jekyll + plugins to GitHub’s versions)
gem "github-pages", group: :jekyll_plugins

# Optional: if you want to use Sass/SCSS locally
gem "sass-embedded"
